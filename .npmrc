# pnpm 配置文件

# 依赖提升配置 - 使用 hoisted 模式解决 ESLint 插件查找问题
# 这确保 ESLint 插件可以在子包中正确找到
node-linker=hoisted

# 严格 peer dependencies 检查
strict-peer-dependencies=false
auto-install-peers=true

# lockfile 配置
lockfile=true
