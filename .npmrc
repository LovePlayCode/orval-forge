# NPM Registry Configuration
# registry=https://registry.npmjs.org/

# 如果使用私有 registry，可以配置如下：
# registry=https://your-private-registry.com/

# 自动保存依赖到 package.json
save-exact=true

# 使用 pnpm 作为包管理器
package-manager=pnpm

# 发布配置
# access=public

# Tag 配置
tag-version-prefix=""

# 忽略脚本
ignore-scripts=false
